
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= task && task._id ? "Edit Task" : "Add Task" %> - Task Manager</title>
</head>
<body>
  <div class="task-form-container">
    <h1><%= task && task._id ? "Edit Task" : "Add Task" %></h1>
    <form action="<%= task && task._id ? '/tasks/' + task._id + '?_method=PUT' : '/tasks' %>" method="POST">
      <label for="title">Title</label>
      <input type="text" name="title" id="title" value="<%= task.title || '' %>" required>
      <label for="description">Description</label>
      <textarea name="description" id="description"><%= task.description || '' %></textarea>
      <button type="submit"><%= task && task._id ? "Update" : "Create" %></button>
    </form>
    <a href="/tasks"><button>Back to Dashboard</button></a>
  </div>
</body>
</html>